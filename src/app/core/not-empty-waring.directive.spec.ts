import { NotEmptyWaringDirective } from './not-empty-waring.directive';

describe('NotEmptyWaringDirective', () => {
  it('should create an instance', () => {
    const directive = new NotEmptyWaringDirective();
    expect(directive).toBeTruthy();
  });
});
